# Rust
/target
/vendor
finalOutput
ignore

# VsCode
.vscode

# npm / js
node_modules
dist
bindings
web/api_bindings.ts

# Server
server

# Benchmark run outputs (scripts and profiles live under tools/benchmark)
/benchmark-results

/.idea
