# AGENTS.md — betterparsec 작업 지침

> **이 파일은 이 저장소에서 일하는 모든 코딩 에이전트의 공통 규칙이다** — ChatGPT/DevSpace, Claude Code, Codex, Cursor 등.
> `AGENTS.md`는 여러 에이전트가 자동으로 읽는 사실상 표준 파일이고, DevSpace는 접속 클라이언트가 무엇이든 매 세션 이 파일을 주입한다(앞 32,000자).
> **네 능력(서브에이전트·컨텍스트 크기·기억)은 호스트마다 다르다. 규칙은 능력을 단정하지 않고 "있으면 이렇게, 없으면 이렇게"로 쓴다.**
> 여기와 [HANDOFF.md](HANDOFF.md)에 없는 결정은 다음 세션엔 존재하지 않는다. 규칙을 어기게 되면 조용히 넘기지 말고 사용자에게 알린다.

---

## 0. 세션 시작 — 반드시 이 순서로 (모든 에이전트 공통)

1. **[HANDOFF.md](HANDOFF.md) "지금 상태" 절을 먼저 읽는다.** 현재 진행점·마지막 결정·미해결 리스크가 거기 있다.
2. 건드릴 영역의 **seam 파일을 실제로 `read` 한다.** 파일명·시그니처·동작을 추측으로 쓰지 않는다. 크면 grep/검색으로 좁혀 읽는다.
3. **이번 세션 = 한 논리 단위.** 시작 전 할 일을 3~7줄 계획으로 적는다. 범위 밖은 손대지 않는다.

## 1. 컨텍스트 유지 (맥락 유실 방지 — 모든 에이전트 공통)

대화·세션은 휘발한다. **기억은 대화가 아니라 파일에 남긴다.** (컨텍스트가 작거나 세션 간 기억이 없는 호스트에겐 필수, 그렇지 않은 호스트에도 좋은 위생.)

- **큰 작업은 쪼갠다.** 한 세션에서 끝낼 단위로만 착수한다. 못 끝낼 규모면 계획만 HANDOFF/ROADMAP에 적고 분할한다.
- **세션 종료 전 반드시:** ① [HANDOFF.md](HANDOFF.md) "지금 상태" 갱신 ② [docs/agent/worklog.md](docs/agent/worklog.md)에 `날짜 — 무엇을 했고 다음은 무엇` 한 줄 ③ commit + push (§4).
- **결정·근거·막힌 지점은 즉시 파일에 적는다.** "나중에 정리"는 다음 세션이 못 본다.

## 2. 병렬 · 격리 · 전문화 작업 (호스트 능력에 따라)

- **네이티브 서브에이전트가 있으면(예: Claude Code의 Agent 툴) 적극 활용한다.** 탐색·리뷰·독립 하위작업을 병렬 에이전트로 분리하고, 결론만 받아 합친다. 병렬로 파일을 동시 수정하면 worktree 격리를 쓴다.
- **네이티브 서브에이전트가 없으면(예: ChatGPT/DevSpace) 아래로 대체한다:**
  - **병렬·위험 실험 = 워크트리.** 본 체크아웃을 오염시킬 작업(리팩터, 의존성 bump, 위험 패치)은 새 worktree를 열어 거기서 한다. 워크트리는 격리 수단이지 백업이 아니다 — 끝나면 반드시 커밋한다(§4).
  - **큰 과제 = 서브세션 브리핑.** 큰 과제는 한 대화에 다 넣지 말고, 좁게 스코프한 새 대화를 연다. 첫 메시지에 `AGENTS.md·HANDOFF.md 먼저 읽어라` + 목표 1개 + 건드릴 파일 목록.
- **반복 절차는 어느 호스트든** 즉흥으로 하지 말고 [docs/agent/session-playbook.md](docs/agent/session-playbook.md)를 읽고 그대로 따른다. 새 반복 절차가 생기면 플레이북을 추가한다(나중에 `SKILL.md`로 승격 가능).

### 모델 등급 — 작업의 무게에 비용을 맞춘다

일을 서브에이전트나 하위 세션에 넘길 때, **전부를 최고 성능 모델에 몰지 마라.** 그렇다고 전부를 싼 모델에 맡기지도 마라. 판단 기준은 하나다: **"틀렸을 때 비용이 큰가, 그리고 틀린 걸 알아채기 어려운가."** 그런 일일수록 비싼 모델로, 아니면 싼 모델로 보낸다.

- **비싼(고성능) 모델 — 중요한 "사고" 작업.** 판단·의사결정(어떤 설계를 고를지, 트레이드오프 저울질), 아키텍처·프로토콜 설계(특히 ABR·비트레이트·전송 경로 — 이 프로젝트의 간판이자 회귀 위험이 가장 큰 곳), 검증(어떤 주장·측정·수정이 정말 맞는지 적대적으로 따져보기), 코드·스펙 리뷰와 비평(구멍 찾기), §5 불변 규칙에 닿는 변경. 이런 일은 정답이 미묘하고, 틀려도 겉보기엔 그럴듯해서 **싼 모델이 "자신 있게 틀리기" 딱 좋은** 영역이다. 여기서는 비용을 아끼지 마라 — 여기서 틀리면 뒤에서 몇 배로 문다.

- **싼(경량) 모델 — 단순·반복·대량 작업.** 기계적 수정(대량 rename, 정해진 패턴의 리팩터), 대량 수집·요약(여러 파일 훑어 목록 만들기, 로그·PCAP 파싱, 벤치 결과 표 정리), 보일러플레이트·테스트 스캐폴딩처럼 **정답이 분명하고 검증이 쉬운** 일. 처리량이 목적인 팬아웃(넓게 훑기)은 싼 모델로 넓고 병렬로 돌린다.

- **섞을 땐 이렇게.** 넓게 훑고 긁어모으는 단계는 싼 모델로 병렬 처리하고, 그 결과를 놓고 **무엇을 할지 정하고 합치는 단계만 비싼 모델**에 맡긴다(팬아웃은 싸게, 판단·합성은 비싸게). 그리고 싼 모델의 대량 산출물을 **그대로 믿지 마라** — 테스트·컴파일 같은 자동 체크나 고성능 모델의 스팟 리뷰로 한 번 걸러라. 싼 모델의 진짜 비용은 요금이 아니라 **애매한 일에서의 잘못된 확신**이다.

## 3. 완료 게이트 (품질 — 모든 에이전트 공통)

- **"될 것 같다 / 아마" 금지.** 완료 주장은 **이번 세션의 실제 tool 실행 결과**로만 한다. 실행 안 했으면 "미검증"이라 명시한다.
- 코드를 바꿨으면 **빌드/테스트를 실제로 돌리고 출력을 본다** → 실패하면 고친 뒤 다시 돌린다. 정적으로 "잘 짜였다"는 검증이 아니다.
- **성능 주장은 2대 실측 전까지 금지** ([HANDOFF.md](HANDOFF.md) 리스크 #1). loopback 수치·internal metric을 wire efficiency/input-to-photon으로 오인하지 않는다.
- **커밋 전 diff를 자기 리뷰한다** (`show_changes` / `git diff`). 의도 밖 변경·디버그 잔여물·시크릿이 없는지 확인한다.

## 4. 커밋 · git 규율 (모든 에이전트 공통)

- **논리 단위마다 커밋.** 그리고 **세션 종료 전 반드시** `commit` + `push origin betterparsec`. 미커밋 작업은 워크트리 정리·리셋 한 번에 사라진다.
- 워크트리에서 한 작업도 반드시 커밋해 브랜치/원격으로 남긴다.
- 커밋 메시지 형식: `betterparsec: <무엇을 왜>` (예: `betterparsec: ABR up-slow 램프에 RTT 게이트 추가`).
- upstream 반영은 `git fetch upstream && git merge upstream/master`. 패치 의존성 revision 변경은 [docs/DEPENDENCIES.md](docs/DEPENDENCIES.md) 절차.

## 5. 이 프로젝트 불변 규칙 (절대 위반 금지 — 모든 에이전트 공통)

- **GPL-3.0-or-later.** 카피레프트. 배포 시 소스 공개 의무, 베이스 LICENSE·저작권 표기 유지.
- **TURN/coturn secret을 client artifact(웹 번들·로그·커밋)에 저장 금지.** 단기 credential만 서버가 발급한다.
- **`sent_unacknowledged`(client 관측)와 encoder-applied(host)를 혼동 금지.** 비트레이트가 "적용됐다"는 host log/ACK로만 확정한다.
- **AV1 YUV444는 이 RTX 4070 host에서 미지원** → 지원한다고 광고·구현 가정 금지.
- **규정 리스크:** 사지방 원격접속·필터 우회는 훈령 위반 소지(징계·계정삭제). **기술적 성공 ≠ 사용 허용.** 설계·실사용 전제는 규정 리스크 없는 환경(집 외부망 등).

## 6. 빌드 · 테스트 (근거는 실행으로만 — 모든 에이전트 공통)

- bootstrap(패치 의존성): Windows `pwsh ./tools/bootstrap-dependencies.ps1`, 그 외 `bash tools/bootstrap-dependencies.sh`
- 테스트: `cargo test --workspace --locked`, `npm run test:stats`, 빌드 `npm run build`
- 툴체인: Rust nightly-2026-02-13, Node 24+, PowerShell 7. 상세는 [HANDOFF.md](HANDOFF.md) "빌드·실행·테스트", 의존성 정책은 [docs/DEPENDENCIES.md](docs/DEPENDENCIES.md).

---

_이 파일을 고치면 커밋한다 — 그래야 워크트리와 이후 clone에도 자동 반영된다._
_호스트별 세부 지침이 더 필요하면: Claude Code용은 저장소 루트 `CLAUDE.md`, ChatGPT/DevSpace 세부는 [docs/agent/session-playbook.md](docs/agent/session-playbook.md)에 둔다 — 단, DevSpace는 두 파일을 모든 클라이언트에 주입하므로 특정 호스트에서만 참인 내용은 "…면"으로 조건화한다._
