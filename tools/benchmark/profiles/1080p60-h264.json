{
  "schemaVersion": 1,
  "name": "1080p60-h264",
  "description": "Initial BetterParsec/Parsec comparison cell. Keep every candidate at the same resolution, refresh rate, content trace, and measured wire path.",
  "stream": {
    "width": 1920,
    "height": 1080,
    "fps": 60,
    "codec": "h264",
    "hdr": false,
    "requestedBitrateKbps": 20000,
    "audio": false
  },
  "timing": {
    "warmupSeconds": 30,
    "measurementSeconds": 120,
    "cooldownSeconds": 10
  },
  "validityGates": {
    "requiredIceRoute": "direct",
    "requiredCandidateProtocol": "udp",
    "maximumSenderQueueFrames": 2,
    "maximumHealthyLinkFrameRejectionPercent": 0.5,
    "maximumBrowserDecodeP95Ms": 5,
    "requireZeroHealthyLinkFreezes": true
  },
  "contentTrace": {
    "name": "deterministic-motion-v1",
    "source": "external",
    "notes": "Use an identical pre-recorded or deterministic rendered sequence for every candidate. Record the exact file/hash in the run manifest."
  }
}
